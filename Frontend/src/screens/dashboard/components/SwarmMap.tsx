// Componente órfão: nada em src/ importa `SwarmMap` — `DashboardScreen.tsx`
// usa `SwarmGrid` no lugar dele hoje. Ver nota completa em `AlertList.tsx`
// (mesmo motivo/mesma correção). Seguro apagar este arquivo (e
// `hooks/useSwarmMap.ts`, que só era usado por ele).
export {};
