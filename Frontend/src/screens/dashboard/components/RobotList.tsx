// Componente órfão: nada em src/ importa `RobotList` — `DashboardScreen.tsx`
// usa `ConnectionList` no lugar dele hoje. Ver nota completa em
// `AlertList.tsx` (mesmo motivo/mesma correção). Seguro apagar este arquivo.
export {};
